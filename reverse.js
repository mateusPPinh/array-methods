/**
 * reverse: O método reverse() inverte os itens de um array. O primeiro elemento do array 
 * se torna o último e o último torna-se o primeiro.
 */

const firstOrder = ['one', 'two', 'three']
firstOrder.reverse();

console.log(firstOrder)